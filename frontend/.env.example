# Frontend Configuration

# Backend API URL (optional)
# Leave empty for dynamic detection:
# - Development: http://localhost:4000
# - Production: uses current origin (window.location.origin)
# Only set this if your backend is on a different domain/port
VITE_BACKEND_URL=http://your-backend-url.com
